[package]
name = "ising-monte-carlo"
version = "0.1.0"
edition = "2021"

[dependencies]
crossterm = "0.28"
ratatui = "0.28"
rand = "0.8"
plotters = { version = "0.3", features = ["bitmap_backend"] }
chrono = { version = "0.4", features = ["clock"] }
